# 通义千问API密钥（推荐）
# 获取地址：https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# 模型选择（可选）
# 可选值：qwen-max（最强，成本高）、qwen-plus（推荐，平衡）、qwen-turbo（快速）、qwen-flash（最快）
# 设置为 'auto' 会根据剧本长度智能选择
QWEN_MODEL=qwen-plus

# OpenAI API密钥（备用方案）
# 如果使用OpenAI，需要修改 qwenService.js 中的调用逻辑
OPENAI_API_KEY=your_openai_api_key_here

# 服务器端口
PORT=3002

# 图生视频模型配置（可选）
# 默认使用 wan2.2-i2v-flash（最便宜，0.10元/秒@480P）
# 可选值：
#   - wan2.2-i2v-flash: 极速版，最便宜（480P: 0.10元/秒, 720P: 0.20元/秒, 1080P: 0.48元/秒）
#   - wan2.5-i2v-preview: 预览版（480P: 0.3元/秒, 720P: 0.6元/秒, 1080P: 1元/秒）
#   - wan2.6-i2v: 最新版（720P: 0.6元/秒, 1080P: 1元/秒）
VIDEO_MODEL=wan2.2-i2v-flash

