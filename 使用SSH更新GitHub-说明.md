# 使用 SSH 更新 GitHub 仓库

## 🚀 快速使用

### Windows（CMD 或双击运行）

```cmd
使用SSH更新GitHub.bat
```

### Git Bash 或 Linux

```bash
bash 使用SSH更新GitHub.sh
```

## 📋 脚本执行步骤

1. **检查当前远程仓库配置**
   - 显示当前的远程仓库 URL

2. **切换到 SSH 方式**
   - 将远程仓库 URL 从 HTTPS 改为 SSH
   - `https://github.com/...` → `git@github.com:...`

3. **验证远程仓库配置**
   - 确认已成功切换到 SSH

4. **检查 Git 状态**
   - 显示所有未提交的更改

5. **添加、提交并推送**
   - 添加所有更改
   - 提交更改（提交信息：`chore: 清理多余的脚本和文档，更新README.md，添加Milvus配置说明`）
   - 推送到 GitHub

## ✅ 验证推送是否成功

### 方法1：检查 Git 日志

```bash
git log --oneline -3
```

应该看到最新的提交包含 "chore: 清理多余的脚本和文档"。

### 方法2：在 GitHub 上检查

1. 访问：https://github.com/ZhehuanUnique/AIGC-jubianage-agent
2. 查看最新提交是否包含 "chore: 清理多余的脚本和文档"
3. 检查文件列表，确认新文件已存在

## 🔍 如果推送失败

### 问题1：SSH 密钥未配置

**检查 SSH 密钥：**
```bash
# 测试 GitHub SSH 连接
ssh -T git@github.com
```

**如果失败，需要：**
1. 生成 SSH 密钥（如果还没有）：
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```
2. 将公钥添加到 GitHub：
   - 复制公钥：`cat ~/.ssh/id_ed25519.pub`
   - 在 GitHub 设置中添加 SSH 密钥

### 问题2：网络连接问题

**检查网络：**
```bash
ping github.com
```

### 问题3：权限问题

**检查远程仓库配置：**
```bash
git remote -v
```

应该显示：`git@github.com:ZhehuanUnique/AIGC-jubianage-agent.git`

## 📝 手动执行步骤

如果脚本无法运行，可以手动执行：

```bash
# 1. 切换到 SSH 方式
git remote set-url origin git@github.com:ZhehuanUnique/AIGC-jubianage-agent.git

# 2. 验证配置
git remote -v

# 3. 添加所有更改
git add -A

# 4. 提交更改
git commit -m "chore: 清理多余的脚本和文档，更新README.md，添加Milvus配置说明"

# 5. 推送到 GitHub
git push origin main

# 6. 验证推送
git log --oneline -3
```

## 🎯 下一步

推送成功后，在服务器上执行更新：

```bash
ssh ubuntu@119.45.121.152 "cd /var/www/aigc-agent && git pull origin main && bash 完整更新服务器.sh"
```

---

**创建时间**：2026-01-03  
**最后更新**：2026-01-03

